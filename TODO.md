# 待办事项

> 本文件只记录**尚未完成**的工作；条目完成后即删除，不在本文件中保留历史（历史见 `CHANGELOG.md`）。

## 总目标：双模式改造 + 启动命令改名

最终形态为**两种互不混合**的运行模式，由启动参数与终端环境自动判定：

| 模式 | 触发条件 | 面向对象 | 核心行为 |
|------|----------|----------|----------|
| **纯 stdio 模式** | 带参数，**或** stdin/stdout 非 TTY（管道、重定向） | 脚本、管道、shell 兜底钩子 | **无任何额外交互**；读入文本，把**最终 content 以纯文本写出 stdout** |
| **TUI 模式** | **无参数且 stdin/stdout 均为 TTY** | 人类用户 | 全屏交互界面，可借鉴知名项目（见 §3） |

同时，启动命令由 `aiz` / `zen` 改为 **`ai`**。

## 前置事项

- [ ] 提交当前工作区改动（0.7.0：对话内命令提示 + 命令清单单一来源），作为双模式改造的基线

## 1. 纯 stdio 模式（P0）

### 1.1 行为约定

- [ ] **不进入交互循环**：不启动 inquirer 主循环、不显示欢迎横幅、不等待任何按键
- [ ] **输入来源**（自动判定，无需额外开关）：
  - [ ] 位置参数存在 → 参数拼接为提示词
  - [ ] stdin 被重定向（`!process.stdin.isTTY`）→ 读取全部 stdin 文本
  - [ ] 两者同时存在 → 参数为指令、stdin 为内容（合并语义见 §5 开放问题 2）
- [ ] **输出**：仅将**最后一条 assistant 消息的 content** 以纯文本写入 stdout
  - [ ] 多 content 块时只取文本块；非文本块（图片等）的处理方式见 §5 开放问题 3
  - [ ] stdout **零装饰**：无 ANSI 颜色、无 emoji 前缀、无 spinner、无进度条
- [ ] **通道分离**：日志、进度、工具调用过程、错误摘要一律走 **stderr**
- [ ] **退出码**：成功 `0`；模型/网络/配置错误 `非 0`
- [ ] **落盘策略**：默认不写草稿、不写对话存档（是否需要 `--save` 之类的显式开关见 §5 开放问题 4）
- [ ] shell 兜底钩子改走本模式：`command_not_found_handle(){ ai "$@"; }`，在用户当前 shell 会话中就地输出答案

### 1.2 验收标准

- [ ] `echo "你好" | ai > out.txt` → `out.txt` 为纯文本，且 `grep -c $'\x1b' out.txt` 为 `0`
- [ ] `cat 大文件 | ai "总结要点" > summary.txt` → 正常工作，stdout 仅含结果
- [ ] `ai "一句话介绍你自己" > a.txt` 全程无交互提示、无按键等待
- [ ] `ai "问题" 2>/dev/null` → 丢弃 stderr 后仍得到完整结果
- [ ] 模型报错时退出码非 0，且错误只出现在 stderr
- [ ] 在非 TTY 环境下，**任何**参数组合都不会进入 TUI

## 2. TUI 模式（P1）

### 2.1 硬性要求

- [ ] 仅在**无参数且 stdin/stdout 均为 TTY** 时启动；CI、管道、重定向一律回落 stdio 模式（判定可参照 Ink 的 `!isInCi && stdout.isTTY`）
- [ ] **终端状态安全**：SIGINT / SIGTERM / 未捕获异常路径都必须恢复 raw mode、光标与备用屏幕，不留「花屏终端」
- [ ] **Ctrl+C 语义**：仅取消当前请求（不退出、不残留 raw mode）；空输入下 Ctrl+C / Ctrl+D 才退出
- [ ] 终端缩放、CJK 与 emoji 宽度、tmux / SSH 环境下显示正确

### 2.2 能力清单

- [ ] v1：流式输出区 + 多行输入（Enter 提交、Ctrl+J 换行）
- [ ] v1：内联 `/` 命令菜单（↑↓ 选择、Enter 确认、Tab 补全），取代现有 `src/slash-hint-prompt.ts` 的底栏提示
- [ ] v1：状态栏（模型、token 用量、Agent）
- [ ] v1：历史滚动
- [ ] 后续：`@` 文件引用补全、会话内搜索与回滚、工具调用折叠面板、图片/表格渲染、主题

### 2.3 可借鉴项目（已核验技术栈）

| 项目 | 技术栈 | 借鉴点 |
|------|--------|--------|
| Gemini CLI | Ink + React | slash 补全：`useSlashCompletion` 产出 `Suggestion[]` + `SuggestionsDisplay` 组件 |
| Codex CLI | ratatui + crossterm | 输入区与弹出菜单的布局分层 |
| Crush | Bubble Tea + Bubbles + Lipgloss | 组件化输入框、状态栏 |
| aider | prompt_toolkit | 行式编辑器 + 补全菜单的混合模型（非全屏） |
| Claude Code | 闭源 | 交互形态与快捷键习惯 |

### 2.4 验收标准

- [ ] 无参数 + TTY → 进入 TUI；带参数或管道 → 绝不进入 TUI
- [ ] 长会话下流式输出不闪屏、不撕裂
- [ ] `kill -TERM` 后终端恢复正常；`Ctrl+C` 取消请求后仍可继续对话

## 3. 启动命令改名 `ai`（P0）

- [ ] `package.json` 的 `bin` 由 `{ aiz, zen }` 改为 `ai`
- [ ] 是否保留 `aiz` / `zen` 作为过渡别名 → 见 §5 开放问题 1
- [ ] shell 钩子函数体由 `aiz "$@"` 改为 `ai "$@"`；为已安装用户提供迁移路径（`ai hook uninstall && ai hook install`）与提示
- [ ] 全量替换文档与提示文案中的 `aiz` / `zen`（README 中英、`docs/`、源码内提示语句；`CHANGELOG.md` 历史记录不回改）
- [ ] 排查 `ai` 与常见 shell 别名、已有工具的命名冲突，必要时在 README 中给出提示
- [ ] 版本策略：属破坏性变更，发布版本号与迁移说明 → 见 §5 开放问题 6

## 4. 共用改造

- [ ] 入口路由：`src/index.ts` 拆分为「模式判定 + 两套入口」，判定逻辑单测覆盖
- [ ] 输出通道拆分：`stdout` 只承载结果，`stderr` 承载日志/进度/工具过程
- [ ] 流式渲染：stdio 模式直接按块写出文本；TUI 模式纳入其渲染模型
- [ ] 草稿与迁移能力在两种模式下的归属界定（stdio 模式默认不落盘，见 §5 开放问题 4）
- [ ] `src/slash-hint-prompt.ts` 与 inquirer 主循环的去留：TUI 模式下被取代、stdio 模式下不使用，明确保留还是删除
- [ ] e2e：新增 stdio 模式管道测试（`echo x | ai`、退出码、stdout 纯净性）；TUI 以 pty 冒烟测试覆盖
- [ ] 文档：README（中/英）补充双模式说明与示例；`docs/` 增补模式判定与迁移章节

## 5. 开放问题（需决策）

1. `ai` 是否保留 `aiz` / `zen` 过渡别名（建议保留一个版本周期）？
2. stdio 模式下「参数 + stdin」同时存在时的合并语义（参数为指令、stdin 为内容，还是互斥报错）？
3. stdio 模式中非文本 content 块（图片 URL 等）如何输出？
4. stdio 模式是否落盘草稿/对话存档（默认不落盘，是否需要显式开关）？
5. TUI 技术选型：以 Ink 重写，还是在现有 inquirer 体系上渐进增强（前者的交互上限更高，后者的迁移成本更低）？
6. TUI 是否启用备用屏幕（影响终端原生滚动与鼠标复制）？
7. 版本与发布节奏：一次性大版本，还是「命令改名 → stdio 模式 → TUI 模式」分阶段发布？

## 非目标

- stdio 模式下不渲染任何 TUI 元素（含底栏提示、菜单、状态栏）
- 不改变 Agent / MCP / Skill / 子 Agent / 任务迁移的既有语义
- 不做 Web / 桌面端形态（本次仅 CLI 双模式）
